#!/usr/bin/env python3

import noise

#  gridsize = 4


# # Linearly interpolate between A and B
# def lerp(a, b, w):
#     assert w >= 0 and w <= 1, 'Invalid weight w=%d' % w
#     return (1-w)*a + w*b

# def get_grid_coord(c):
#     c0 = c//gridSize
#     if c < 0:
#         c0 -= 1
#     c0 *= gridSize
#     return c0

# def get_gradient(x, y):
#     # TODO
#     return (0, 0)

# def dot_grid_gradient(x0, y0, x, y):
#     gradient = get_gradient(x, y)
#     distance = x-x0, y-y0
#     return distance[0]*gradient[0] + distance[1]*gradient[1]

# def perlin(x, y):
#     # Determine grid cell coordinates (units in pixels)
#     x0 = get_grid_coord(x)
#     x1 = x0+gridSize
#     y0 = get_grid_coord(y)
#     y1 = y0+gridSize
    
#     # Determine interpolation weights
#     weightX = x-x0
#     weightY = y-y0
 
#     # Perform teh interpolation!

if __name__ == '__main__':
    pass
  
